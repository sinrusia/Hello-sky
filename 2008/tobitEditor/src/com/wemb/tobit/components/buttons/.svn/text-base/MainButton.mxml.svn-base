<?xml version="1.0" encoding="utf-8"?>
<s:Group xmlns:fx="http://ns.adobe.com/mxml/2009"
		 xmlns:s="library://ns.adobe.com/flex/spark"
		 xmlns:mx="library://ns.adobe.com/flex/mx"
		 buttonMode="true" click="clickHandler(event)" mouseChildren="false" useHandCursor="true">
	
	<fx:Metadata>
		[Event(name="btnClick", type="flash.events.Event")]
		[Style(name="bollet", inherit="no", type="Class")]
		[Style(name="rollOverLabelColor", inherit="no", type="uint")]
	</fx:Metadata>
	
	<fx:Script>
		<![CDATA[
			import com.wemb.tobit.utils.Util;
			import com.wemb.tobit.vo.MenuInfo;
			
			//---------------------------------
			// Variables
			//----------------------------------
			[Bindable] 
			public var text:String = "";
			
			public var index:int = 0;
			
			public var menuInfo:MenuInfo;
			
			private var _selected:Boolean = false;
			
			private var _isChangeSelected:Boolean = false;
			
			//---------------------------------
			// Setter / Getter
			//----------------------------------
			public function get selected():Boolean
			{
				return _selected;
			}
			
			public function set selected(value:Boolean):void
			{
				_isChangeSelected = true;
				_selected = value;
				
				if ( !_selected )
				{
					menu_lbl.setStyle("color", getStyle("color"));
				}
				
				invalidateProperties();
			}
			
			//---------------------------------
			// Override
			//----------------------------------
			override protected function createChildren():void
			{
				super.createChildren();
				
				this.addEventListener(MouseEvent.ROLL_OVER, onRollOver);
				this.addEventListener(MouseEvent.ROLL_OUT, onRollOut);
			}
			
			override protected function commitProperties():void
			{
				super.commitProperties();
				
				if( _isChangeSelected )
				{
					_isChangeSelected = false;
				}
			}
			
			override public function styleChanged(styleName:String):void
			{
				super.styleChanged(styleName);
				if(imgToggle) {
					imgToggle.source = this.getStyle("bollet");
				}
				menu_lbl.setStyle("fontFamily", this.getStyle("fontFamily"));
			}
			
			
			//---------------------------------
			// Function
			//----------------------------------
			private function onRollOver(event:MouseEvent):void
			{
				menu_lbl.setStyle("color", getStyle("rollOverLabelColor"));
			}
			
			private function onRollOut(event:MouseEvent):void
			{
				if( _selected )
				{
					menu_lbl.setStyle("color", getStyle("rollOverLabelColor"));
				}
				else
				{
					menu_lbl.setStyle("color", getStyle("color"));
				}
			}
			
			private function clickHandler(e:Event):void
			{
				selected = true;
				dispatchEvent(new Event("btnClick"));
			}
		]]>
	</fx:Script>
	
	<s:layout>
		<s:HorizontalLayout gap="3" verticalAlign="baseline"/>
	</s:layout>
	
	<s:Image id="imgToggle"/>
	
	<s:Label id="menu_lbl" text="{text}"/>
		
</s:Group>
